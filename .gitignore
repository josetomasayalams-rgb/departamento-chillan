# Basura del sistema
.DS_Store

# No parte de la app (artefacto sin referencia en el código)
ruvector.db

# Foto fuente de 17 MB (no se usa; el fondo optimizado es assets/chillan-bg.jpg)
oficial-nevados_*.jpg

# Artefacto de build local para debug (app canónica = index.html)
calendario.html

# Repo independiente; nunca debe entrar como submódulo del calendario familiar.
chillan-rentas/

# Supabase CLI local state and secrets
supabase/.temp/
supabase/.env

# Logical production backups created before migrations
backups/
